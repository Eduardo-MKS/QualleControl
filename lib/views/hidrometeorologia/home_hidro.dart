import 'package:flutter/material.dart';

class HomeHidro extends StatelessWidget {
  const HomeHidro({super.key});

  @override
  Widget build(BuildContext context) {
    // ignore: avoid_unnecessary_containers
    return Scaffold(
      appBar: AppBar(title: Text('HidroMeteorologia')),
      body: Center(child: Text('HidroMeteorologia')),
    );
  }
}
